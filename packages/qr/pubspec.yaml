name: amiro_qr
description: QR generation and scanning for Amiro identity sharing.
version: 0.1.0
publish_to: none
environment:
  sdk: ">=3.9.0 <4.0.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^4.0.0
