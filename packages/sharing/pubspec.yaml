name: sharing
description: Amiro identity sharing orchestration (payload build/parse, no platform imports).
version: 0.1.0
publish_to: none
environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  identity_core:
    path: ../identity_core

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
